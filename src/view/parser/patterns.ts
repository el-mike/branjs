export const NAMESPACE_PREFIX = 'bn';

export const PATTERNS = {
  SELECTOR: `${NAMESPACE_PREFIX}-selector`,
  IF: `${NAMESPACE_PREFIX}-if`,
  FOR: `${NAMESPACE_PREFIX}-for`,
  INPUT: `${NAMESPACE_PREFIX}-input`,
  OUTPUT: `${NAMESPACE_PREFIX}-output`
};
